{
  "name": "mantle",
  "version": "1.0.0",
  "description": "A high-end platform for corporate productivity and workforce relief.",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "install-all": "npm install --workspace=client --workspace=server"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
